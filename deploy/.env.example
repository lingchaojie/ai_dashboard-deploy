# gateway.sh install generates .env; later commands preserve your settings.
GATEWAY_IMAGE=ghcr.io/lingchaojie/ai_dashboard:latest
BIND_HOST=127.0.0.1
SERVER_PORT=8080
COMPOSE_PROJECT_NAME=ai-gateway
GATEWAY_UID=10001
GATEWAY_GID=10001
TZ=Asia/Shanghai
# Optional: initialize the administrator before enabling public HTTPS.
DOMAIN=
